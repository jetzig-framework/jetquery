pub const database = .{
    .testing = .{
        .adapter = .postgresql,
        .database = "postgres",
        .username = "postgres",
        .hostname = "127.0.0.1",
        .password = "password",
        .port = 5432,
    },
};
